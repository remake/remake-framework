// default framework options

let remakeOptions = {
  // this will attach a unique id to EVERY object in your data
  autoGenerateUniqueIds: true,
  // this will generate a nicely formatted JSON file representing each user's data in the /tempData directory
  writeAppDataToTempFiles: true
};

export { remakeOptions };